POSTGRES_URL=postgres://user:password@localhost:5432/trippi

# Base URL used for links in emails (preferred over VERCEL_URL because it's explicit)
APP_URL=http://localhost:3000

# Resend
RESEND_API_KEY=
RESEND_FROM="Trippi <[email protected]>"

# Email link signing secret (generate with: openssl rand -base64 32)
EMAIL_LINK_SECRET=

# Protect cron endpoint (generate with: openssl rand -base64 32)
CRON_SECRET=

# Local testing: if set (and not production), cron emails will only be sent to this address.
EMAIL_TEST_TO=


