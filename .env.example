# This is an example of your .env file format.
# Note: this must be .env, not .env.local, without further configuration changes.

# Postgres connection string used by Drizzle and the app server.
POSTGRES_URL=postgresql://user:password@localhost:5432/circle_feed
